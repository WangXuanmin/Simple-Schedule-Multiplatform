# API Service

Recommended first implementation:

- Node.js HTTP API
- TypeScript
- Hosted PostgreSQL as the required cloud database
- Supabase Postgres as the first provider

The API owns account data, task storage, sync cursors, and conflict resolution.

See `../../docs/cloud-postgres.md` for schema and connection rules.
